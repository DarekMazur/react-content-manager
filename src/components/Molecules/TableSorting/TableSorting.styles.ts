import styled from 'styled-components';

export const StyledTableSorting = styled.div`
  display: flex;
  flex-direction: column;
  padding-left: 1rem;
  font-size: 1rem;

  svg {
    cursor: pointer;
  }
`;
